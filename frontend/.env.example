# Only needed when the backend is NOT reachable through the dev proxy
# (vite.config.ts proxies /api -> http://127.0.0.1:8000 during `npm run dev`).
# Set this for a production build pointing at a deployed API.
# VITE_API_BASE=https://your-backend.example.com
